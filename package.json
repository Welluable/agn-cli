{
  "name": "@welluable/agn-cli",
  "version": "0.0.3",
  "description": "A yolo coding agent simple enough to fit in your head.",
  "author": "manojsinghnegi",
  "license": "MIT",
  "type": "module",
  "bin": {
    "agn": "dist/cli.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc && chmod +x dist/cli.js",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "example": "tsx --env-file=.env",
    "prepublishOnly": "npm run build",
    "publish": "./dist/cli.js 'bump the npm version and publish' ",
    "push": "./dist/cli.js 'commit in commitzen style andpush the latest version to the remote repository' "
  },
  "keywords": [
    "ai",
    "agent",
    "cli",
    "coding-agent",
    "llm"
  ],
  "engines": {
    "node": ">=18"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/welluable/agn-cli.git"
  },
  "dependencies": {
    "chalk": "^5.6.2",
    "openai": "^4",
    "yaml": "^2.8.4"
  },
  "devDependencies": {
    "tsx": "^4.21.0",
    "typescript": "^5.8.0",
    "vitest": "^4.1.5"
  }
}
